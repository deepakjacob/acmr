import * as React from "react";
import { Button } from '@blueprintjs/core/dist';

export const ChartConfigWidget = () => (
    <div className="chart-header">
        <span>VALUE  (Y-AXIS)</span>
        <Button iconName="cog" style={{}} >Configure</Button>
    </div >
);
